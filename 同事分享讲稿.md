# OpenCode 高级用法：同事分享讲稿

## 分享目标

让同事通过一个简单的 Spring Boot 折扣 API 理解：OpenCode 不只是聊天式代码生成器，也可以作为面向软件研发场景的 AI Agent 开发框架和执行引擎。

建议时长：约 40 分钟，其中讲解约 30 分钟、演示约 10 分钟。

## 1. 开场：我们真正想解决什么（2 分钟）

可以这样开场：

> 大多数人第一次使用编码 Agent 时，只是在输入框中描述需求。这种方式可以工作，但结果严重依赖当时写了多少上下文。OpenCode 的高级用法，是把这些上下文和工作方式固化在仓库里，让团队成员获得更一致、可审计、可复用的结果。

强调三个问题：

1. Agent 不知道团队的架构和命令。
2. Agent 默认角色过于宽泛。
3. 重复任务每次都要重新描述。

## 2. 换一个视角：OpenCode 也是 Agent 开发框架（4 分钟）

过渡话术：

> 前面我们把 OpenCode 当作一个开发工具。再换一个视角看，它其实已经提供了 Agent 开发所需的大部分基础设施：模型调用、工具循环、会话管理、角色定义、知识加载、权限控制和事件接口。因此，我们不仅可以使用 OpenCode，还可以在它上面开发自己的研发 Agent 产品。

为什么可以把它视为 Agent 框架：

- Agent：定义角色、系统提示词、模型和权限。
- `AGENTS.md` 与 Skill：提供长期项目知识和按需操作手册。
- Tool 与 MCP：让模型调用代码仓库、数据库和内部系统。
- Session、Message 与 Event：管理任务状态、上下文和流式进度。
- Permission：把 Agent 能做什么变成可执行的安全规则。
- Server 与 OpenAPI：允许其他应用把 OpenCode 当作无界面执行引擎。

结合本次 Spring Boot 示例，可以采用下面的架构：

```text
前端或内部平台
      ↓
Spring Boot 控制层
鉴权、任务队列、Prompt 模板、审计、结果持久化
      ↓ HTTP / SSE
OpenCode Server
Session、Agent、Skill、权限、工具调用循环
      ↓
代码仓库 / Maven / MCP / 内部服务
```

可以照着讲：

> 在这个架构里，Spring Boot 是控制面，负责用户、任务和企业系统集成；OpenCode 是执行面，负责让模型理解仓库、选择 Agent、调用工具并持续完成任务。Java 服务不必重新实现一套 Agent Loop，只需要通过 OpenCode 的 Session、Message 和 Event API 管理任务。

典型调用过程：

1. Spring Boot 为任务准备独立工作目录或 Git worktree。
2. 调用 OpenCode Server 创建 Session，并发送包含 Agent 和 Prompt 的 Message。
3. OpenCode 加载仓库规则和 Skill，按权限调用文件、Shell、Custom Tool 或 MCP。
4. Spring Boot 通过 SSE 接收事件，向前端展示进度，并保存最终结果和代码差异。

适合的场景：代码审查、测试生成、自动修复、仓库问答、版本检查和研发自动化。

边界也要讲清楚：OpenCode 的优势集中在代码仓库和开发工具链。如果目标是通用客服、强业务流程编排或跨天运行的审批工作流，可以让专门的业务编排框架负责流程，把 OpenCode 作为其中的“研发 Agent 执行节点”。

落地建议：Java 项目优先使用 `opencode serve` 加 HTTP/OpenAPI 集成；官方 SDK 主要面向 JavaScript/TypeScript。V2 嵌入式 SDK 目前仍是 Beta，不建议直接作为关键生产系统的稳定接口。

参考：[Server](https://opencode.ai/docs/server/)、[SDK](https://opencode.ai/docs/sdk/)、[Custom Tools](https://opencode.ai/docs/custom-tools/)。

## 3. 可选增强层：oh-my-opencode-slim（4 分钟）

过渡话术：

> 如果说原生 OpenCode 给了我们搭建 Agent 的积木，那么 oh-my-opencode-slim 给的是一套已经组装好的多 Agent 团队。它不是另一个 OpenCode，而是运行在 OpenCode 之上的第三方编排插件。

用一张表说明两者关系：

| 层次 | 负责什么 |
| --- | --- |
| OpenCode | 模型、会话、工具、权限、Server 等 Agent 运行时能力 |
| oh-my-opencode-slim | 专业 Agent 团队、自动委派、后台并行、模型预设和内置 Skill |
| 项目配置 | 团队自己的 `AGENTS.md`、中文 Prompt、权限和验收规则 |

它内置的主要分工包括：

- Orchestrator：拆解任务、选择 Agent、跟踪并汇总结果。
- Explorer：快速搜索代码库。
- Oracle：处理架构、复杂调试和技术判断。
- Librarian：查询外部文档和依赖资料。
- Designer：处理 UI/UX 和前端任务。
- Fixer：执行范围明确的代码修改。
- Council：让多个模型并行分析后综合结论。

可以照着讲：

> oh-my-opencode-slim 的价值不是 Agent 数量多，而是它把任务路由做成了现成方案。强模型负责规划和判断，快速模型负责搜索和明确实现，团队还可以跨厂商混用模型，在质量、速度和成本之间做分层。

为什么叫可选增强层：

- 优点：减少从零设计多 Agent 编排的成本，适合快速验证复杂研发工作流。
- 代价：增加第三方依赖、配置复杂度、并发调用和 Token 消耗。
- 建议：先掌握原生 OpenCode，再在非关键仓库试用；不要把插件和自动委派当成所有任务的默认答案。

如果现场已经安装，可以输入：

```text
ping all agents
```

然后用 Spring Boot 示例演示：

```text
分析如何为折扣接口增加固定金额优惠。让 Explorer 定位代码，让 Oracle 评估金额精度和兼容性，最后给出实现与测试计划。暂时不要修改代码。
```

如果现场没有安装，只展示配置示例即可，不建议临时修改全局环境：

- [`Oh My OpenCode Slim介绍.md`](./Oh%20My%20OpenCode%20Slim介绍.md)
- [`oh-my-opencode-slim 配置示例`](./opencode-demo/config-snippets/oh-my-opencode-slim/)

收束话术：

> 原生 OpenCode 适合自己精确搭建；oh-my-opencode-slim 适合快速获得一套有明确分工的多 Agent 工作方式。它是框架之上的加速器，而不是框架本身的替代品。

## 4. 一张图讲清配置层次（3 分钟）

展示下面这张表：

| 层次 | 解决的问题 | 示例 |
| --- | --- | --- |
| `AGENTS.md` | Agent 应该长期知道什么 | 架构、规范、验证命令 |
| Agent | 谁来完成任务 | architect、reviewer、debugger |
| Command | 怎样快速启动固定流程 | `/review`、`/test-fix` |
| Skill | 复杂流程如何按需加载 | git-release、incident-debugging |
| MCP | 如何连接外部系统 | GitHub、数据库、内部平台 |
| Tool | 如何暴露项目专用能力 | 查询项目信息、内部校验 |
| Plugin | 如何监听和扩展生命周期 | 日志、通知、环境文件保护 |

核心表达：

> Rules 是知识，Agent 是角色，Command 是入口，Skill 是操作手册，MCP/Tool 是能力，Plugin 是生命周期扩展。

## 5. AGENTS.md：团队共享的工程上下文（3 分钟）

打开 [`opencode-demo/AGENTS.md`](./opencode-demo/AGENTS.md)。

先用一分钟介绍示例项目：它是 Java 21 + Spring Boot 4.1.1 的折扣计算 REST API，包含 `PricingController`、`PricingService`、Jakarta Validation、统一异常处理、服务层单元测试和 MockMvc 接口测试。

说明应该写进去的内容：

- 不容易从文件名看出来的架构信息。
- 确切的 Maven 编译、针对性测试和完整验证命令。
- 修改后的验证顺序。
- 安全边界，例如禁止读取 `.env`、禁止发布和部署。

不建议写进去的内容：

- 大段通用编程常识。
- 很快会过期的临时任务。
- 密钥和内部凭证。
- 可以直接从代码看出来的逐文件说明。

## 6. Agent：让不同角色拥有不同权限（4 分钟）

依次展示：

- [`architect.md`](./opencode-demo/.opencode/agents/architect.md)：只读设计。
- [`reviewer.md`](./opencode-demo/.opencode/agents/reviewer.md)：只读审查。
- [`debugger.md`](./opencode-demo/.opencode/agents/debugger.md)：允许运行诊断命令。
- [`implementer.md`](./opencode-demo/.opencode/agents/implementer.md)：允许受控编辑。
- [`docs.md`](./opencode-demo/.opencode/agents/docs.md)：只处理文档。

模型配置可以直接写在 Agent Markdown 的 frontmatter 中。展示 [`agent-model-routing/architect.md`](./opencode-demo/config-snippets/agent-model-routing/architect.md)：

```yaml
model: openai/gpt-5.6-sol
reasoningEffort: high
```

没有配置 `model` 时，子 Agent 继承调用它的主 Agent 模型。团队可以让 architect、reviewer 和 debugger 使用强推理模型，让 implementer 使用编码模型，让 docs 使用更快、更经济的模型。模型 ID 必须以 `opencode models` 的实际输出为准。

强调：不要只通过提示词说“请勿修改”，要用权限真正限制工具。

现场调用示例：

```text
@architect 分析如何给 Spring Boot 折扣接口增加固定金额优惠，暂时不要修改代码
```

```text
@reviewer 审查当前未提交改动
```

## 7. Command 与 Skill：把工作流产品化（4 分钟）

展示 [`commands/`](./opencode-demo/.opencode/commands/)：

- `/plan-feature`：规划功能。
- `/review`：审查当前 diff。
- `/test-fix`：运行测试并修复。
- `/explain`：通过本地只读 architect 解释指定代码范围。
- `/release-check`：调用发布 Skill。

说明 Command 适合短流程入口，而 Skill 适合较长、按需加载的操作手册。

展示：

- [`git-release/SKILL.md`](./opencode-demo/.opencode/skills/git-release/SKILL.md)
- [`incident-debugging/SKILL.md`](./opencode-demo/.opencode/skills/incident-debugging/SKILL.md)

## 8. 权限：从“信任提示词”变成“约束执行能力”（4 分钟）

打开 [`opencode.jsonc`](./opencode-demo/opencode.jsonc)。

解释三种权限：

- `allow`：自动执行。
- `ask`：执行前确认。
- `deny`：禁止执行，`--auto` 也不能绕过。

重点展示：

- 查询代码和 Git 状态自动允许。
- 修改文件需要确认。
- `rm`、`git push`、`mvn deploy`、部署命令直接禁止。
- MCP 示例默认关闭。
- 自动更新和会话分享默认关闭，保证演示更可控。

提醒同事不要把未经筛选的 `opencode debug config` 输出贴进工单、聊天或演示页面，因为合并后的全局 MCP 配置可能包含凭证。

## 9. MCP、Custom Tool 和 Plugin（3 分钟）

说明三者边界：

- MCP：连接外部产品或远程服务，但工具太多会占上下文。
- Custom Tool：用少量代码暴露本项目专用能力。
- Plugin：监听工具、会话和文件事件，执行横切逻辑。

展示：

- [`config-snippets/local-mcp.jsonc`](./opencode-demo/config-snippets/local-mcp.jsonc)
- [`tools/project.ts`](./opencode-demo/.opencode/tools/project.ts)
- [`plugins/env-protection.js`](./opencode-demo/.opencode/plugins/env-protection.js)

## 10. 自动化（2 分钟）

展示两个脚本：

- [`headless-review.sh`](./opencode-demo/scripts/headless-review.sh)
- [`server-review.sh`](./opencode-demo/scripts/server-review.sh)

说明 `opencode run --format json` 可以接入脚本；大量重复调用时，可以连接常驻 `opencode serve`，减少 MCP 冷启动。

## 11. 现场演示（10 分钟）

按照 [`现场演示步骤.md`](./现场演示步骤.md) 操作。

推荐只演示下面四件事：

1. 让 `architect` 输出只读方案。
2. 让 Build/implementer 完成一个小改动并运行 Maven 测试。
3. 用 `/review` 检查 diff。
4. 展示危险命令被权限阻止。

## 12. 收尾

最后可以这样总结：

> OpenCode 既可以作为个人编码工具，也可以作为团队开发研发 Agent 的框架选择。oh-my-opencode-slim 则是在此基础上快速获得多 Agent 编排的可选加速层。落地时先理解原生能力和安全边界，再决定哪些团队需要引入自动委派。

建议团队第一周完成：

1. 给一个真实项目提交 `AGENTS.md`。
2. 建立只读 reviewer。
3. 建立 `/review` 和 `/test-fix`。
4. 配置危险操作 deny 清单。
5. 收集一周使用反馈后再扩展。
