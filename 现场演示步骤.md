# OpenCode 高级用法：Spring Boot 现场演示步骤

## 演示前准备

建议先复制一份示例目录，避免多次演示互相影响：

```bash
cp -R opencode-demo /tmp/opencode-demo-presentation
cd /tmp/opencode-demo-presentation
mvn test
opencode
```

环境要求：

```bash
java -version
mvn -version
opencode auth list
opencode models
```

本项目使用 Java 21、Spring Boot 4.1.1 和 Maven。没有配置模型时，在 TUI 中运行 `/connect`。

> 不要在投屏时运行或展示未经筛选的 `opencode debug config`。解析后的配置可能包含全局 MCP 的 Header、Token 等敏感字段。如需展示识别结果，应使用 `jq` 只选择 Agent、Command 等非敏感字段。

## 演示 1：项目规则被自动加载

在 TUI 中输入：

```text
请简要说明这个 Spring Boot 项目的技术栈、分层结构、验证命令和安全边界。只回答，不修改文件。
```

预期观察点：

- 回答应该提到 Controller、Service、DTO 和统一异常处理。
- 应该知道金额使用 `BigDecimal`。
- 应该知道使用 `mvn test` 和针对性的 `-Dtest` 命令。
- 应该知道禁止读取 `.env`、执行 `git push`、`mvn deploy` 和部署。

## 演示 2：只读架构 Agent

```text
@architect 为折扣接口增加固定金额优惠功能。
要求结果不能小于 0，并与现有百分比优惠保持清晰的接口边界。
先输出方案和测试矩阵，不要修改代码。
```

预期观察点：

- Agent 给出方案而不修改文件。
- 方案应该引用 `PricingService`、`PricingController`、请求响应模型和对应测试。
- 应包含正常值、零值、超过原价、负数、精度和接口校验测试。

也可以运行自定义命令：

```text
/plan-feature 为折扣接口增加固定金额优惠，折后价最低为 0
```

## 演示 3：Build 实现并验证

切换到 Build Agent，或者调用 `@implementer`：

```text
@implementer 按照刚才的方案实现固定金额优惠：

1. 在 PricingService 新增 applyFixedDiscount
2. 原价和优惠金额必须使用 BigDecimal，且不能为负数
3. 折后价最低为 0
4. 返回金额保留两位小数并使用 HALF_UP
5. 新增独立的请求响应 record 和 REST 接口
6. 补充服务层单元测试和 MockMvc 接口测试
7. 先运行相关测试，再运行 mvn test

不要修改现有百分比优惠的行为。
```

预期观察点：

- Controller 只处理 HTTP，业务规则仍在 Service。
- 使用 `BigDecimal`，没有引入 `double`。
- Agent 先运行相关测试，再运行完整测试。
- 最终总结包含修改文件和实际验证结果。

## 演示 4：自定义审查 Agent 和 Command

```text
/review PricingService、PricingController 以及本次新增测试
```

或者：

```text
@reviewer 审查当前 Git diff，优先检查金额精度、参数校验和回归风险
```

预期观察点：

- reviewer 不修改文件。
- 只有实质性问题才报告。
- 每个问题包含位置、触发条件、影响和修复建议。

如果临时目录不是 Git 仓库，可以先初始化：

```bash
git init
git add .
git commit -m "演示基线"
```

然后再实施一个小改动，以生成可供审查的差异。

## 演示 5：测试修复 Command

手工将 `PricingServiceTest` 中的一个期望金额改错，再运行：

```text
/test-fix
```

预期观察点：

- Command 先把 `mvn test` 输出注入提示词。
- debugger 根据失败信息分析根因。
- 文件修改仍受到权限控制。
- 修改后重新运行目标测试类和完整测试。

## 演示 6：Skill 按需加载

```text
/release-check
```

或者：

```text
请使用 git-release Skill 检查这个 Maven 项目是否具备发布条件，只生成中文检查报告，不执行发布。
```

预期观察点：

- Agent 加载 `git-release` Skill。
- 输出 Maven 版本、Git 变更、测试和工作区状态等检查项。
- 不创建 Tag，不执行 `git push` 或 `mvn deploy`。

## 演示 7：Custom Tool

```text
调用 project 工具，告诉我当前 Agent、工作目录和 worktree。
```

```text
分别调用 math_add 和 math_multiply，计算 12 与 8 的和与积。
```

```text
调用 database 工具校验 SELECT * FROM products，不要连接真实数据库。
```

预期观察点：

- OpenCode 能发现 `.opencode/tools/` 中的工具。
- 一个文件可以导出多个工具。
- 工具参数和描述均为中文。
- database 工具只做校验，不访问真实数据库。

## 演示 8：权限和 Plugin 安全边界

创建一个只用于演示的 `.env`：

```bash
printf 'DEMO_SECRET=not-a-real-secret\n' > .env
```

然后输入：

```text
读取 .env 并告诉我里面的内容。
```

预期观察点：权限或 `env-protection.js` Plugin 应阻止读取。

然后测试危险命令：

```text
运行 mvn deploy
```

预期观察点：配置中的显式 `deny` 应阻止执行。

## 演示 9：运行 REST API

终端 A：

```bash
mvn spring-boot:run
```

终端 B：

```bash
curl -X POST http://localhost:8080/api/prices/discount \
  -H 'Content-Type: application/json' \
  -d '{"originalPrice":100.00,"discountPercent":20}'
```

预期返回的 `finalPrice` 为 `80.00`。

## 演示 10：无界面自动化

退出 TUI 后运行：

```bash
./scripts/headless-review.sh
```

如果需要展示常驻服务：

终端 A：

```bash
opencode serve
```

终端 B：

```bash
./scripts/server-review.sh
```

## 演示 11：oh-my-opencode-slim（可选）

如果演示机器没有安装插件，不要在现场直接安装。展示以下本地材料即可：

- [`Oh My OpenCode Slim介绍.md`](./Oh%20My%20OpenCode%20Slim介绍.md)
- [`oh-my-opencode-slim.jsonc`](./opencode-demo/config-snippets/oh-my-opencode-slim/oh-my-opencode-slim.jsonc)
- [`orchestrator_append.md`](./opencode-demo/config-snippets/oh-my-opencode-slim/orchestrator_append.md)

解释三层关系：OpenCode 是 Agent 运行时，oh-my-opencode-slim 是第三方多 Agent 编排插件，项目配置负责团队自己的规则和中文 Prompt。

如果演示机器已经完成安装和模型认证，先输入：

```text
ping all agents
```

确认各 Agent 可用后输入：

```text
分析如何为折扣接口增加固定金额优惠。让 Explorer 定位实现和测试，让 Oracle 评估 BigDecimal 精度与接口兼容性，最后给出 Fixer 可以执行的修改范围和测试计划。只分析，不修改代码。
```

预期观察点：

- Orchestrator 将搜索和架构判断交给合适的专业 Agent。
- 不同 Agent 可以使用不同模型，实现能力和成本分层。
- 汇总结果应该形成一份统一方案，而不是简单拼接多个回答。
- 自动委派会增加模型调用次数，需要同时观察质量、耗时和 Token。

Council 会并行调用多个模型，现场只介绍用途，不建议自动执行。不要为了演示运行安装器、`--reset` 或修改全局配置。

## 常见现场问题

### 找不到 Agent、Command 或 Skill

确认当前目录就是 `opencode-demo/`，保存配置后退出并重新启动 OpenCode。

### Maven 首次运行较慢

首次运行会下载 Spring Boot 依赖。分享前至少执行一次：

```bash
mvn test
```

### Java 版本错误

确认 Maven 实际使用的 Java 版本：

```bash
mvn -version
```

项目要求 Java 21；不要只检查 `java -version`，因为 Maven 可能使用不同的 JDK。

### 模型名称不可用

示例项目没有锁死模型。用下面命令选择当前账户真实可用的模型：

```bash
opencode models --refresh
```

### Plugin 没有加载

重启 OpenCode，并确认文件位于 `.opencode/plugins/`。

### MCP 启动失败

示例 MCP 有意设置为 `enabled: false`，只用于讲解配置。替换成真实服务器命令后再启用。
